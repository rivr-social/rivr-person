/**
 * Type augmentation for @react-three/fiber intrinsic elements.
 *
 * React 19 + @react-three/fiber v8 has a known issue where the JSX namespace
 * augmentation does not properly extend JSX.IntrinsicElements. This declaration
 * registers the r3f elements used in our components.
 */

import "@react-three/fiber";

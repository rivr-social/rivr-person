/**
 * Default placeholder image paths used throughout the application.
 *
 * Centralises fallback image references so they can be updated in one place
 * instead of being scattered as magic strings across 70+ files.
 */

/** Default avatar for users / people when no image is available. */
export const DEFAULT_AVATAR = "/placeholder-user.jpg";

/** Default generic placeholder (typically used for group/org avatars). */
export const DEFAULT_COVER = "/placeholder.svg";

/** Default image for events and content cards. */
export const DEFAULT_EVENT_IMAGE = "/placeholder-event.jpg";

/** Default image for groups. */
export const DEFAULT_GROUP_IMAGE = "/placeholder-group.jpg";
